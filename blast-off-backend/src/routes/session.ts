import express from 'express';
const router = express.Router();

/* GET home page. */
router.route('/')
    .post((req, res) => {
        // TODO: implement
        res.status(403);
    })
    .delete((req, res) => {
        // TODO: implement
        res.status(403);
    });

export default router;
